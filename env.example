# Environment Configuration for SafeHome Backend
# Copy this file to .env and fill in your values

# Node Environment (development, production, test)
NODE_ENV=development

# Server Port (Render automatically sets PORT, but you can override)
PORT=5000

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?appName=AppName
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/SafeHomeChild?appName=Cluster0

# JWT Secret Key (Use a strong random string in production)
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Origin - Frontend URL(s)
# For single origin: https://your-frontend.vercel.app
# For multiple origins: https://your-frontend.vercel.app,http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# Alternative: You can also use FRONTEND_ORIGIN
# FRONTEND_ORIGIN=http://localhost:3000

# Location Data Retention (days)
LOCATION_RETENTION_DAYS=30

# Map Visualization - MapLibre GL (RECOMMENDED: FREE, Open-Source, Best Features)
# MapLibre is free, open-source, and Mapbox-compatible with excellent features
# No API key required!
NEXT_PUBLIC_MAP_LIBRARY=maplibre

# Alternative Options (if needed):
# Option 1: Leaflet + OpenStreetMap (Simple, free, no API key)
# NEXT_PUBLIC_MAP_LIBRARY=leaflet

# Option 2: Mapbox (PAID - requires API key)
# NEXT_PUBLIC_MAP_LIBRARY=mapbox
# NEXT_PUBLIC_MAPBOX_KEY=pk.your-mapbox-token

# Option 3: Google Maps (FREE tier: $200/month credit, requires API key)
# NEXT_PUBLIC_MAP_LIBRARY=google
# NEXT_PUBLIC_GOOGLE_MAPS_KEY=your-google-maps-api-key

